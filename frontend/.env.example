EXPO_PUBLIC_API_URL=
# Provide a full URL or just the host/IP for your backend.
# Override only when the backend listens on a non-default port.
EXPO_PUBLIC_API_PORT=
EXPO_PUBLIC_API_URL=http://localhost:5000
